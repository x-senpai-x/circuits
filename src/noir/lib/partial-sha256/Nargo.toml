[package]
name = "partial_sha256"
type = "lib"
authors = ["ProveKit Team"]
compiler_version = ">=1.0.0"

[dependencies]
utils = { path = "../utils" }
poseidon = { tag = "v0.1.1", git = "https://github.com/noir-lang/poseidon" }
